{
  "name": "organizational-workflows-docs",
  "version": "1.0.0",
  "private": true,
  "description": "Documentation site for the Organizational Workflows solutions (MkDocs + custom theme).",
  "scripts": {
    "build": "node run.js",
    "build:headless": "node run.js --headless",
    "preview": "node run.js --preview",
    "dev": "node run.js --dev"
  }
}
